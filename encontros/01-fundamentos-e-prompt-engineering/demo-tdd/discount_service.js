/**
 * Utilitário de Cálculo de Descontos (Para desafio TDD)
 * 
 * Este arquivo deve conter uma função que calcula descontos progressivos:
 * - 5% para compras acima de R$ 100
 * - 10% para compras acima de R$ 500
 * - 15% para compras acima de R$ 1000
 */

function calculateDiscount(totalValue) {
  // A ser implementado via TDD
}

module.exports = { calculateDiscount };
